# MetaHub 项目规则

本文件是 MetaHub 项目的项目级 Codex / Agent 规则。若与上级通用规则冲突，以本文件为准。

## 交付闭环

- 后续每次完成一个功能后，必须同步更新相关文档，包括但不限于任务开发书、PRD、README、迁移说明、API 说明或验收记录中受影响的部分。
- 功能完成并通过相关验证后，必须提交代码并推送到 GitHub 远端仓库：`https://github.com/Lynn-Lee/MetaHub`。
- 提交前必须检查 `git status --short` 和本次 diff，确保只包含当前功能相关改动，不提交 `.env`、真实凭据、临时文件、缓存或构建产物。
- 推送前必须运行与改动范围匹配的验证命令；如测试、类型检查、迁移或本地依赖不可用，必须在最终回复中说明原因和剩余风险。
- 若远端、认证或网络导致无法推送，必须明确报告阻塞点、已完成的本地提交 SHA，以及可重试的命令。
